# Development Notes

## Cli Runtime Testing

To run the CLI runtime in test mode, use the following command:

```bash
### arguments interactive input:
npm run test:cli:dev
### arguments i
npm run test:cli <command> [options]
```

This will execute the CLI with in either development mode, allowing you to enter command line arguments interactively, 
or with specified command and options.

